## Packages
framer-motion | Complex animations for the dashboard and landing page
date-fns | Date formatting for bookings and calendars
react-day-picker | Calendar component for booking dates

## Notes
Auth flow uses cookie-based sessions via /api/user, /api/login, /api/register
Images are currently static placeholders until file upload is implemented
Dashboard uses a sidebar layout with role-based navigation
